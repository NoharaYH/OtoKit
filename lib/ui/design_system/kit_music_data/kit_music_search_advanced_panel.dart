import 'package:flutter/material.dart';
import '../constants/sizes.dart';

/// 曲库/宴谱高级检索展开区域。
/// 单独注册为组件，当前仅占位，暂无内容。
class KitMusicSearchAdvancedPanel extends StatelessWidget {
  const KitMusicSearchAdvancedPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: UiSizes.spaceL);
  }
}
