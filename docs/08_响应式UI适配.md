# 响应式 UI 适配

## 架构红线

- `WindowSizeClass`、`DeviceTopology`、Pane 切分结果只允许存在于 `ui/` 层闭环
- `application/` 层绝不感知宽度分档、拓扑、导航呈现模式
- 业务页绝不直接依赖 `WindowSizeClass`、`DeviceTopology`、`displayFeatures` 原始值
- `ThemeExtension` 是 UI 层唯一合法的跨层渲染 DI 机制
- 响应式信息只通过 `ResponsiveLayoutScope`（InheritedWidget）下发

---

## 架构红线：平板与手机严格分离

**原则**：平板状态与手机状态在断点、状态、组件三方面严格区分，禁止混写、禁止手机路径被平板逻辑污染。

### 1. 断点唯一来源

| 约束 | 说明 |
|------|------|
| **唯一定义处** | 宽度断点（600 / 840 / 1200 dp）仅在 `layout_analyzer.dart` 的 `analyzeLayout()` 内定义，全项目禁止在其他处硬编码等价断点做「是否平板/手机」判断 |
| **下游消费** | 业务与壳层只消费 `ResponsiveLayoutScope` 下发的 `isCompactNavigation`、`primaryPaneWidth`、`availablePaneCount`，不直接读 `MediaQuery.sizeOf(context).width` 做分档 |
| **RootPage 例外** | 当前 RootPage 用 `width < 600` 决定 `showGlassOverlay` 属于历史遗留；建议后续改为从 Scope 或 Shell 提供语义，统一断点数据源 |

### 2. 状态严格分离

| 形态 | 状态归属 | 可见范围 |
|------|----------|----------|
| **手机 (Compact)** | 导航/浮层状态由 `NavigationProvider` 提供（如 `isDeckOpen`、`currentTag`） | 全树可见，但仅 Compact 路径使用 deck 相关字段 |
| **平板 (Medium+)** | 侧边栏状态由 `TabletSidebarController` 提供（open/close/expand/collapse/leaving） | **仅**在 `OtokitResponsiveShell._buildExpandedLayout` 子树内 `ChangeNotifierProvider`，手机树不可见、不依赖 |

禁止将平板侧边栏状态（如 isOpen、isExpanded、side）放入 `NavigationProvider` 或任何全局单例，导致手机路径可访问或随其 `notifyListeners()` 重建。

### 3. 组件与布局路径严格分离

| 约束 | 说明 |
|------|------|
| **分支唯一发生点** | 手机 vs 平板的分支**仅**在 `OtokitResponsiveShell.build` 内：`isCompact ? _buildCompactLayout : _buildExpandedLayout`，两条子树互不交叉 |
| **手机专属** | `_buildCompactLayout` 内：`NavDeckOverlay`、左侧 4% 手势热区、右上角 `_buildActionButtons(showMenu: true)`。上述组件与逻辑不得出现在 Expanded 路径 |
| **平板专属** | `_buildExpandedLayout` 内：`TabletSidebarController`、`_TabletExpandedLayout`（玻璃层+偏移动画）、两侧点击热区、`TabletSidebarMinimal`、玻璃内设置按钮。上述组件与逻辑不得出现在 Compact 路径 |
| **PageShell** | 只做「背景 + 毛玻璃底板」，不判断设备形态、不包含侧边栏/热区/玻璃平移，不依赖 `TabletSidebarController` 或平板状态。`showGlassOverlay` 由调用方传入 |
| **kit_* 组件** | 共享的 `kit_*` 组件（如 `KitNavCapsule`）禁止内部根据 `MediaQuery.width >= 600` 等做平板/手机分支；形态差异由壳层用不同组件或不同参数在两条路径分别提供 |
| **业务页** | 仅通过 `ResponsiveLayoutScope.maybeOf(context)` 获取 `primaryPaneWidth`、`availablePaneCount`、`isCompactNavigation` 做布局决策，不直接判宽度断点 |

### 4. 验收标准

- **手机零改动**：新增或修改平板能力时，`_buildCompactLayout` 及其使用的组件（如 `NavDeckOverlay`、`KitNavCapsule` 在 deck 中的用法）相对手机基线零 diff。
- **平板状态隔离**：手机路径 build 中不出现 `Consumer<TabletSidebarController>` 或对平板侧边栏状态的依赖。
- **断点单源**：除文档允许的 RootPage 临时例外外，全项目仅 `layout_analyzer.dart` 内存在 600/840/1200 的宽度分档逻辑。

---

## 文件职责总览

| 文件 | 职责 |
|---|---|
| `ui/design_system/constants/layout_analyzer.dart` | **断点唯一定义处**。纯函数，输入宽度 + displayFeatures，输出 WindowSizeClass / DeviceTopology / hingeBounds（600/840/1200 仅在此处定义） |
| `ui/design_system/constants/responsive_layout_scope.dart` | InheritedWidget，向子树下发 availablePaneCount / primaryPaneWidth / isCompactNavigation（**平板/手机区分的下游唯一来源**） |
| `ui/design_system/otokit_responsive_shell.dart` | 根级壳层，读取 MediaQuery，调用分析器，**唯一分支点** isCompact → _buildCompactLayout / _buildExpandedLayout，渲染手机或平板导航 |
| `ui/design_system/kit_navigation/tablet_sidebar_controller.dart` | **平板专属状态**。侧边栏 open/close/expand/collapse/leaving，仅在 _buildExpandedLayout 子树内 provide |
| `ui/design_system/kit_navigation/tablet_sidebar_minimal.dart` | **平板专属组件**。Medium+ 下侧边栏 UI，仅由 Shell 的 _buildExpandedLayout 挂载 |
| `ui/design_system/kit_navigation/nav_deck_overlay.dart` | **手机专属组件**。Compact 下浮层胶囊导航，仅由 _buildCompactLayout 挂载 |
| `ui/pages/score_sync/components/score_sync_assembly.dart` | Score Sync 业务组件，消费 ResponsiveLayoutScope.availablePaneCount 切换单/双栏布局 |
| `ui/pages/music_data/components/mai_music_assembly.dart` | MaiMai 曲目数据页，宽屏下限制内容区最大宽度 |
| `ui/pages/music_data/components/chu_music_assembly.dart` | Chunithm 曲目数据页，同上 |

---

## 布局参数

```
WindowSizeClass:
  compact   < 600dp
  medium    600–839dp
  expanded  840–1199dp
  large     ≥ 1200dp

Pane 分配（Flat 设备）:
  compact / medium  → 1 pane
  expanded / large  → 2 panes

导航模式（严格分离，见架构红线）:
  compact   → NavDeckOverlay（浮层胶囊）+ 左侧手势热区；状态 NavigationProvider
  medium+   → TabletSidebarMinimal（侧边栏）+ 玻璃层偏移动画 + 两侧热区；状态 TabletSidebarController

UiSizes.getHorizontalMargin 档位:
  primaryPaneWidth > 840  → spaceXL (32dp)
  primaryPaneWidth > 600  → spaceL (24dp)
  otherwise               → screenEdgeMargin (12dp)

物理折叠屏退让阈值:
  _minPaneWidth = 280dp
```

---

## Phase 1：根级壳层收口

**目标**：将响应式判断从业务页中剥离，统一收口到 Shell 层。

### 核心逻辑（`otokit_responsive_shell.dart`）

```
MediaQuery → analyzeLayout() → LayoutAnalysis
                                    ↓
                         _resolvePaneCount()  →  availablePaneCount
                         primaryWidth 计算    →  primaryPaneWidth
                                    ↓
                         ResponsiveLayoutScope（InheritedWidget 下发）
                                    ↓
              isCompact ? _buildCompactLayout : _buildExpandedLayout
```

### `analyzeLayout` 纯函数（`layout_analyzer.dart`）

输入：`widthDp`、`displayFeatures`  
输出：`LayoutAnalysis { sizeClass, topology, hingeBounds }`

- 遍历 `displayFeatures` 筛选 `hinge` / `fold` 类型
- 0 个铰链 → `flat`，1 个 → `singleHinge`，≥2 个 → `dualHinge`

### `ResponsiveLayoutScope` 下发字段

| 字段 | 类型 | 说明 |
|---|---|---|
| `availablePaneCount` | `int` | 当前可用 Pane 数（1 / 2 / 3） |
| `primaryPaneWidth` | `double` | 主区域实际可用宽度（dp） |
| `isCompactNavigation` | `bool` | 是否 Compact 导航模式 |

业务页通过 `ResponsiveLayoutScope.maybeOf(context)` 消费，返回 nullable，无 Shell 环境时自动退化。

---

## Phase 2：Score Sync 双栏试点

**目标**：Score Sync 页在平板/宽屏下切换为难度选择器与日志面板并排的双栏布局。

### 改动文件：`score_sync_assembly.dart`

**`_buildFormView`**：外包 `Center` + `ConstrainedBox(maxWidth: 480)`，宽屏下表单不无限拉宽。

**`_buildSuccessView`**：读取 `availablePaneCount`，分两条路径：

```
availablePaneCount >= 2 → Row
  ├─ Expanded(flex: 2)  →  Column(header + difChoice)
  └─ Expanded(flex: 3)  →  SyncLogPanel（estimatedUsedHeight: 0）

availablePaneCount == 1 → Column（原有布局不变）
  ├─ header
  ├─ difChoice
  └─ Expanded → SyncLogPanel（estimatedUsedHeight: 正常值）
```

### 关键设计

- `SyncLogPanel` 的 `ValueKey('Log_${widget.gameType}')` 在两条路径下均保留，State 在布局切换时天然保活，无需额外处理
- `maybeOf` 而非 `of`，无 Shell 环境时退化为单栏
- 薄包装层 `mai_sync_page.dart`、`chu_sync_page.dart` 不动

---

## Phase 3：Music Data 宽度约束

**目标**：Music Data 页在宽屏下限制内容区宽度，防止列表过度拉伸。

### 改动文件

**`mai_music_assembly.dart`**：`build` 最外层包裹 `Center` + `ConstrainedBox(maxWidth: 600)`，覆盖加载态、无数据态、列表态三种路径。

**`chu_music_assembly.dart`**：在已有的 `Center` 内插入 `ConstrainedBox(maxWidth: 600)`，`Column` 占位符保持 `const`。

### 约束

- 此阶段不引入 `availablePaneCount` 分支
- Master-Detail 分栏结构预留，但不提前实施

---

## Phase 4：物理折叠屏接入

**目标**：为铰链设备激活 280dp 退让合并规则，防止物理分区过窄时强行切出不可用的 Pane。

### 改动文件：`otokit_responsive_shell.dart`

新增顶层私有常量与函数：

```dart
const double _minPaneWidth = 280.0;

int _resolvePaneCount(LayoutAnalysis analysis, double totalWidth)
```

### 退让规则

**Flat 设备**：沿用原逻辑（compact/medium → 1，expanded/large → 2）。

**SingleHinge**：
```
leftZone  = hinge.left
rightZone = totalWidth - hinge.right

两者均 ≥ 280dp → paneCount = 2
任一 < 280dp   → paneCount = 1（退让合并）
```

**DualHinge**：
```
zone1 = h0.left
zone2 = h1.left - h0.right
zone3 = totalWidth - h1.right

validCount = zones.where(z >= 280dp).length
paneCount  = validCount.clamp(1, 3)
```

### `primaryWidth` 修正

退让后（`paneCount == 1`）`primaryWidth` 直接返回 `totalWidth`，不再读铰链坐标：

```dart
if (paneCount == 1) {
  primaryWidth = totalWidth;                          // 退让后全宽
} else if (topology != flat && hingeBounds.isNotEmpty) {
  primaryWidth = hingeBounds.first.left;              // 物理铰链左缘
} else {
  primaryWidth = totalWidth * _defaultPrimaryRatio;   // 软比例切割
}
```

---

## 消费示例

业务页内读取响应式信息：

```dart
final scope = ResponsiveLayoutScope.maybeOf(context);
final paneCount = scope?.availablePaneCount ?? 1;

if (paneCount >= 2) {
  // 双栏布局
} else {
  // 单栏布局
}
```

`getHorizontalMargin` 动态读取 `primaryPaneWidth`，无需业务页手动计算边距：

```dart
// UiSizes.getHorizontalMargin(context) 内部实现
final w = ResponsiveLayoutScope.maybeOf(context)?.primaryPaneWidth ?? 0;
if (w > 840) return spaceXL;
if (w > 600) return spaceL;
return screenEdgeMargin;
```
